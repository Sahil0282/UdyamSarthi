# Runtime dependencies for the API container, pinned from the working venv.
# Deliberately excludes sentence-transformers/torch: PLP retrieval by declared
# section number needs neither, and the semantic fallback degrades cleanly.
# Install requirements.txt instead if you need to rebuild the PLP index.
fastapi==0.141.1
uvicorn==0.52.4  # uvicorn[standard] extras come via the base pin
python-multipart==0.0.32
pydantic==2.13.5
sqlalchemy==2.0.52
psycopg==3.3.5
psycopg-binary==3.3.5
geoalchemy2==0.20.0
pandas==3.0.5
numpy==2.4.6
geopandas==1.1.4
pyogrio==0.13.0
shapely==2.1.2
pyproj==3.7.2
pyyaml==6.0.3
python-dotenv==1.2.3
pymongo==4.18.0
google-genai==2.22.0
